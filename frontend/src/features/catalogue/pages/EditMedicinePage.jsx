import { useEffect,useState } from 'react';import { useNavigate,useParams } from 'react-router-dom';import MedicineForm from '../components/MedicineForm';import { getMedicine,updateMedicine } from '../api/catalogueApi'
export default function EditMedicinePage(){const{medicineId}=useParams();const nav=useNavigate();const[item,setItem]=useState();const[error,setError]=useState('');useEffect(()=>{getMedicine(medicineId).then(setItem).catch(e=>setError(e.message))},[medicineId]);const save=async v=>{try{await updateMedicine(medicineId,v,item.version);nav('/catalogue')}catch(e){setError(e.message)}};return <main className="min-h-screen bg-[#f4f8ff] px-6 py-10"><div className="mx-auto max-w-4xl"><h1 className="mb-6 text-3xl font-bold">Edit medicine</h1>{error&&<p className="mb-4 rounded-lg bg-red-50 p-4 text-red-700">{error}</p>}{item?<MedicineForm initial={item} onSubmit={save}/>:!error&&<p>Loading medicine…</p>}</div></main>}

