import { useState } from 'react';import { useNavigate } from 'react-router-dom';import MedicineForm from '../components/MedicineForm';import { createMedicine } from '../api/catalogueApi'
export default function AddMedicinePage(){const nav=useNavigate();const[busy,setBusy]=useState(false);const[error,setError]=useState('');const save=async v=>{setBusy(true);try{await createMedicine(v);nav('/catalogue')}catch(e){setError(e.message)}finally{setBusy(false)}};return <main className="min-h-screen bg-[#f4f8ff] px-6 py-10"><div className="mx-auto max-w-4xl"><h1 className="mb-6 text-3xl font-bold text-[#0a192f]">Add medicine</h1>{error&&<p className="mb-4 rounded-lg bg-red-50 p-4 text-red-700">{error}</p>}<MedicineForm onSubmit={save} busy={busy}/></div></main>}
