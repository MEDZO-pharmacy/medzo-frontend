export default function InventoryTable({ items }) {
  if (!items.length) return <div className="rounded-2xl bg-white p-10 text-center shadow-sm"><h2 className="font-bold text-[#0a192f]">No inventory records</h2><p className="mt-2 text-slate-500">Add medicines and receive stock to begin tracking inventory.</p></div>
  const status = (item) => <span className={`rounded-full px-3 py-1 text-sm font-semibold ${item.isLowStock ? 'bg-red-50 text-red-700' : 'bg-green-50 text-green-700'}`}>{item.isLowStock ? 'Low stock' : 'Healthy'}</span>
  return <div className="overflow-hidden rounded-2xl bg-white shadow-sm"><div className="grid gap-3 p-3 sm:hidden">{items.map((item) => <article key={item.medicineId} className="rounded-xl border border-slate-200 p-4"><div className="flex items-start justify-between gap-3"><div><h2 className="font-bold text-[#0a192f]">{item.name}</h2><p className="text-sm text-slate-500">{item.genericName}</p></div>{status(item)}</div><dl className="mt-4 grid grid-cols-3 gap-3 text-sm"><div><dt className="text-slate-500">On hand</dt><dd className="text-lg font-bold">{item.quantityOnHand}</dd></div><div><dt className="text-slate-500">Threshold</dt><dd className="text-lg font-bold">{item.reorderThreshold}</dd></div><div><dt className="text-slate-500">Next expiry</dt><dd className="font-semibold">{item.nextExpiry || '—'}</dd></div></dl></article>)}</div><div className="hidden overflow-x-auto sm:block" role="region" aria-label="Inventory records" tabIndex="0"><table className="min-w-[700px] w-full text-left"><thead className="bg-slate-50 text-sm text-slate-500"><tr>{['Medicine', 'Generic name', 'Quantity', 'Threshold', 'Next expiry', 'Status'].map((heading) => <th scope="col" className="p-4" key={heading}>{heading}</th>)}</tr></thead><tbody>{items.map((item) => <tr className="border-t border-slate-100" key={item.medicineId}><td className="p-4 font-bold">{item.name}</td><td className="p-4">{item.genericName}</td><td className="p-4">{item.quantityOnHand}</td><td className="p-4">{item.reorderThreshold}</td><td className="p-4">{item.nextExpiry || '—'}</td><td className="p-4">{status(item)}</td></tr>)}</tbody></table></div></div>
}
