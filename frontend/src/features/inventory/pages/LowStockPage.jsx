import { useEffect,useState } from 'react';import LowStockTable from '../components/LowStockTable';import { getLowStock } from '../api/inventoryApi'
export default function LowStockPage(){const[data,setData]=useState({items:[]});const[error,setError]=useState('');useEffect(()=>{getLowStock().then(setData).catch(e=>setError(e.message))},[]);return <main className="min-h-screen bg-[#f4f8ff] px-6 py-10"><div className="mx-auto max-w-7xl"><h1 className="mb-2 text-3xl font-bold">Low-stock medicines</h1><p className="mb-6 text-[#4a5568]">Items below their reorder threshold.</p>{error?<p className="rounded-lg bg-red-50 p-4 text-red-700">{error}</p>:data.items.length?<LowStockTable items={data.items}/>:<p className="rounded-2xl bg-white p-10 text-center text-[#6b7280]">No medicines are currently below threshold.</p>}</div></main>}
